<Window x:Class="CRUDMaster.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Title="Menu Principal" Height="450" Width="800">
    <StackPanel>
        <Button Content="Pessoas" Command="{Binding AbrirPessoasCommand}" Margin="10"/>
        <Button Content="Produtos" Command="{Binding AbrirProdutosCommand}" Margin="10"/>
        <Button Content="Pedidos" Command="{Binding AbrirPedidosCommand}" Margin="10"/>
    </StackPanel>
</Window>